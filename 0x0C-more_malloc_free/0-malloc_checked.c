#include "main.h"

/**
 * malloc_checked - allocates a memory
 * @b: size
 * Return: the memory
 */
void *malloc_checked(unsigned int b)
{
	int *str;

	str = malloc(b);
	if (s == NULL)
		exit(98);

	return (s);
}

#include "main.h"
/**
 * _abs - print absolute value of an integer
 * @x: parameter to modify
 * Return: absolute value
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
